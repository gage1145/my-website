```{r, echo=FALSE, results='asis'}
files <- list.dirs("templates")[-1] |>
    lapply(list.files, full.names=TRUE) |>
    unlist()
sprintf("{{< include %s >}}\n", files) |>
    cat()
cat('<div id="windows"></div>')
```